import React, { useState } from 'react';
import Header from './Header';

function Form() {
    const [addForm, setAddForm] = useState('');

    return(
        <Header />

    )
};

export default Form;